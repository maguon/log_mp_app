import httpRequest from '../../util/HttpRequest'
import * as reduxActionTypes from '../../reduxActionTypes'

export const getPickUpAddrList = req => (dispatch, getState) => {
    try {

    } catch (err) {
        
    }
}

